let person = {
    name: 'Ahmed',
    age: 28,
    location: 'Cairo'
}

console.log(`${person.name} is ${person.age} years old. He lives in ${person.location}`)

person.age = person.age + 1 

console.log(`${person.name} is ${person.age} years old. He lives in ${person.location}`)
